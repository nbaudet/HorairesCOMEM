<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.comem.horairescomem"
    android:versionCode="1"
    android:versionName="0.1 Beta"
    android:installLocation="internalOnly" >
    
    <uses-sdk
        android:minSdkVersion="11"
        android:targetSdkVersion="19" />

    <application
        android:label="@string/app_name"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:theme="@style/HoWiBaseTheme">
        
        <!-- <activity android:label="@string/app_name"
            android:name=".Configuration" >
        </activity>-->
        
        <!-- Une balise receiver permet d'indiquer un récepteur de diffusion
        qui s'exécutera à chaque fois qu'une intention correspondante sera
        diffusée par le système ou une application.
        Permet de donner les informations concernant l'horaire à la vue -->
        <receiver android:name="ch.HoraireWidgetProvider">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <!-- This specifies the widget provider info -->
            <meta-data android:name="android.appwidget.provider"
                    android:resource="@xml/widgetinfo" />
        </receiver>

        <!-- Le service qui fournit les nouveaux horaires à intervalles réguliers -->
        <service android:name="ch.HoraireWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS"
            android:exported="false" />

        <!-- Une balise provider permet d'indiquer les fournisseurs de contenu de
        notre application.
        Permet de donner le contenu des horaires -->
        <provider android:name="ch.comem.HoraireDataProvider"
              android:authorities="ch.comem.horairelistwidget.provider" />
        
        <activity android:name="ch.comem.Configuration" >
            <intent-filter>
                <action android:name="android.apwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        
    </application>

</manifest>
